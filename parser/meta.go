package parser

func (p *Parser) processmeta() {
	//for range time.Tick(time.Minute * 1) {
	//	var domains []models.Domain
	//	res := p.DB.Model(models.Parse{}).Limit(10).Find(&domains)
	//	if res.Error != nil {
	//		log.Println(res.Error)
	//	} else {
	//		for _, d := range domains {
	//			p.Parse(d)
	//		}
	//	}
	//}
}
